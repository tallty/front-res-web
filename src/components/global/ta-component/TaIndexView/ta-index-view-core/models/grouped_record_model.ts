import { VApi, VModel, VObject, VStore } from '@/lib/vails';
import { ref } from 'vue';

export class GroupedRecordModel extends VModel<VObject> {
  // fetch = () => {
  //   this.store?.index();
  // };
  // records = ref<VObject[]>([]);
}
