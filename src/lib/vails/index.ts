import VApi from './api';
import { VModel } from './model';
import type { VObject } from './model';
import { VStore } from './store';

export default {
  Api: VApi,
  Model: VModel,
  Store: VStore,
};

export { VApi, VModel, VStore, VObject };
